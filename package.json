{
  "name": "token-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/preflight-deploy.mjs && wrangler deploy",
    "cf-typegen": "wrangler types",
    "test": "node --test test/*.test.mjs",
    "logos": "node scripts/build-provider-logos.mjs",
    "logos:check": "node scripts/build-provider-logos.mjs --check",
    "preflight": "node scripts/preflight-deploy.mjs"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.4.0",
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@lobehub/icons-static-svg": "^1.95.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.134.0"
  }
}
